[sandboxes]
default_provider = "docker"

[sandboxes.docker]
enabled = true
# memory_limit = "2g"
# cpu_limit = 2.0

[sandboxes.openshell]
enabled = false
# OpenShell compatibility disabled by default unless requested.
